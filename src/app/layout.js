import { Inter } from "next/font/google";
import { DM_Sans } from "next/font/google";
import { Space_Grotesk } from "next/font/google";
import "./globals.css";
import { EmailJSContactProvider } from "@/ContextAPI/EmailJSContextAPIContact.jsx";
import { EmailJSProvider } from "@/ContextAPI/EmailJSContextAPI.jsx";

const inter = Inter({ subsets: ["latin"] });

const DM_Sans_init = DM_Sans({
  subsets: ["latin"],
  weight: ["100", "200", "300", "400", "500", "600", "700", "800", "900"],
  variable: "--font-dmsans",
});

const Space_Grotesk_init = Space_Grotesk({
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700"],
  variable: "--font-spacegrotesk",
});

// export const metadata = {
//   title: "Esaviour",
//   description: "Generated by Esaviour",
// };

// export const metadata = {
//   metadataBase: new URL("http://localhost:3000"), // Replace with your actual base URL
//   title: "eSaviour | B2B Digital Marketing Agency",
//   description: "The Best B2B Digital Marketing Agency with 3 Years of Experience in Digital Marketing Services.Helped 27+ Brands to Achieve Success.",
//   alternates: {
//     canonical: "./", // Use "./" for the base URL
//   },
// };

export default function RootLayout({ children }) {
  return (
    <html lang="en" data-theme="light">
      <EmailJSProvider>
        <EmailJSContactProvider>
          <body
            className={`${DM_Sans_init.variable} ${Space_Grotesk_init.variable}`}
          >
            {children}
          </body>
        </EmailJSContactProvider>
      </EmailJSProvider>
    </html>
  );
}
